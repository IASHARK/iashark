import {existsSync} from "node:fs";import path from "node:path";import {ApiSports} from "./api/apiSports";import {collectMatchData} from "./data/collectMatchData";import {buildStrength} from "./model/teamStrength";import {runMonteCarlo} from "./model/monteCarlo";import {selectScenario} from "./simulation/selectScenario";import {writeReport} from "./output/report";
async function main(){for(const candidate of [path.resolve("../.env"),path.resolve(".env")])if(existsSync(candidate)){try{process.loadEnvFile(candidate);}catch{}}const key=process.env.API_SPORTS_KEY||process.env.APISPORTS_KEY;if(!key)throw new Error("API_SPORTS_KEY absent. Ajoutez-la dans .env.");const api=new ApiSports(key);const data=await collectMatchData(api);const strength=buildStrength(data);const{aggregates,samples}=runMonteCarlo(data,strength,50000);const scenario=selectScenario(data,samples,"2-2",aggregates.topScorers,"A. Diouf");await writeReport(data,strength,aggregates,scenario);}
main().catch((error)=>{console.error(error instanceof Error?error.message:String(error));process.exitCode=1;});
