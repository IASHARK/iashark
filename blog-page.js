(async function(){'use strict';const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),posts=await fetch('/blog-posts.json').then(r=>r.json()),featured=posts.find(p=>p.featured)||posts[0],categories=['Tous',...new Set(posts.map(p=>p.category))];document.getElementById('featured').innerHTML=`<span class="eyebrow">${esc(featured.category)}</span><h2>${esc(featured.title)}</h2><p class="muted">${esc(featured.excerpt)}</p><footer>${featured.minutes} min de lecture · ${new Date(featured.date).toLocaleDateString('fr-FR')}</footer>`;const filters=document.getElementById('filters'),articles=document.getElementById('articles');filters.innerHTML=categories.map((c,i)=>`<button class="${i?'':'active'}" data-category="${esc(c)}">${esc(c)}</button>`).join('');function render(cat='Tous'){const rows=posts.filter(p=>!p.featured&&(cat==='Tous'||p.category===cat));articles.innerHTML=rows.map(p=>`<article class="card article"><span class="eyebrow">${esc(p.category)}</span><h3>${esc(p.title)}</h3><p>${esc(p.excerpt)}</p><footer>${p.minutes} min de lecture · ${new Date(p.date).toLocaleDateString('fr-FR')}</footer></article>`).join('')}filters.onclick=e=>{if(!e.target.dataset.category)return;filters.querySelectorAll('button').forEach(b=>b.classList.toggle('active',b===e.target));render(e.target.dataset.category)};document.getElementById('popular').innerHTML=posts.slice(0,5).map((p,i)=>`<div class="rank"><b>0${i+1}</b><span>${esc(p.title)}</span><small>${p.minutes} min</small></div>`).join('');render()})();
