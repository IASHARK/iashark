# ONE_OFF_TICKET_OPTIMIZER_V1 — isolated dependencies (not merged into any
# production requirements file). Versions pinned to what this engine was
# built and tested against.
numpy>=1.26
scipy>=1.11
pulp>=2.8
pytest>=7.0  # dev/test only
