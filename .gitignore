.mcp.json
.env
.env.*
*.wsk
.wsk
*.upload_cache.json
odds-snapshots-local.json
__pycache__/
*.pyc
.DS_Store

# Build Netlify (scripts/build-public.js) et config locale d outils
dist/
.codex/

# Dependances locales (Playwright E2E)
node_modules/

# Caches d'API lourds, regenerables, jamais publies (21/09/2026).
# Les fichiers RESTENT sur le disque : git cesse seulement de les suivre.
# Verifie avant retrait : aucun code, aucun test, aucun workflow ne les lit
# (data/player-lab, data/club-hubs et data/league-factory, eux, alimentent
# des tests de l'integration continue et RESTENT suivis).
data/daily-market-scan/
data/analysis/
